


<%= form_for @contractor do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :email %>
  <%= f.text_field :email %>
  <%# <%= f.submit "Submit Contractor", class: 'button', formaction: contractor_path%> 
  <%= f.submit "Submit Contractor", class: 'button'%>
  <%= button_to "Cancel", homes_path, :method => :get, class: 'button'%>
<% end %>