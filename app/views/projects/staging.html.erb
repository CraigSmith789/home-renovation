
<%= form_for([@home, @project]) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>

    <%= f.fields_for :contractor do |c| %>
    <% @contractors.each do |contractor| %>
      <%= contractor.name %>
      <%= contractor.email %>
      
      
    <%= f.submit "Submit Project", class: 'button', formaction: home_projects_path%> <%= f.submit "Cancel", class: 'button'%>
<% end %>

<%= form_for([@home, @project]) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>
  <br>

    <%= f.fields_for :contractor do |c| %>
    <%= c.collection_select :contractor_id, Contractor.all, :id, :name %>
      <%= contractor.name %>
      <%= contractor.email %><hr>
      
      
      
      
    <%= f.submit "Submit Project", class: 'button', formaction: home_projects_path%> <%= f.submit "Cancel", class: 'button'%>
<% end %>